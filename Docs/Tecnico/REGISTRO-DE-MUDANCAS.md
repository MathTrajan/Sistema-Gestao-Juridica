# Registro de Mudanças

Histórico técnico do **Norma — Gestão Jurídica**. Cobre decisões de
arquitetura, correções e mudanças de contrato da API.

Este documento é público. Dados de produção — nomes de clientes, processos,
valores, credenciais — não entram aqui.

Deploy é explícito, por CLI: `vercel --prod` a partir de `norma/`. Push no
GitHub não publica (auto-deploy desligado em `norma/vercel.json`).

---

<!-- AUTO:INSERIR-APOS — publicacoes bem-sucedidas sao gravadas logo abaixo desta linha; nao remova este marcador -->
