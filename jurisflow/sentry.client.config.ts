// Sentry is not installed. To enable:
// 1. npm install @sentry/nextjs
// 2. Set NEXT_PUBLIC_SENTRY_DSN in your environment variables
// 3. Uncomment the block below and withSentryConfig in next.config.ts

// import * as Sentry from '@sentry/nextjs'
// Sentry.init({ dsn: process.env.NEXT_PUBLIC_SENTRY_DSN, tracesSampleRate: 0.1 })

export {}
